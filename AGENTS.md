# Codex 프로젝트 진입 지침

1. 상위 `C:\Users\stuls\Desktop\Agent\law.md`를 가장 먼저 읽는다.
2. `작업내역_체크리스트.md`를 단일 진실 공급원으로 읽고 §6에서 현재 인계 상태를 확인한다.
3. 과제 기준은 `docs/00_과제_요구사항_매핑.md`, 구조는 `docs/01_기획.md`, 검증은 `docs/검증안내서.md`를 본다.
4. 코드 변경 시 관련 문서와 체크리스트를 같은 작업 단위로 갱신한다.
5. `npx tsc -b`, `npm run lint`, `npm run build` 및 Git diff를 확인한다.
6. 다른 AI의 변경을 되돌리지 않고, 커밋·푸시는 사용자 승인 범위에서만 수행한다.
