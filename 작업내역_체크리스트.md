# card-news-maker — 작업내역 체크리스트 (AI 공용 인수인계 파일)

**관련 문서:** `README.md` · `PRD.md` · `CLAUDE.md`

> **이 파일은 여러 AI 모델이 이어달리기로 하나의 프로젝트를 완성하기 위한**
> **유일한 단일 진실 공급원(Single Source of Truth)입니다.**

---

## 0. AI 필독 — 작업 프로토콜 (반드시 이 순서대로)

### ▶ 작업 시작 전 (BEFORE)
1. **이 파일 전체를 먼저 읽는다.**
2. 아래 `6. 다음 작업자에게` 절을 읽고 이어받을 지점을 파악한다.
3. `3. 진행사항 체크리스트`에서 가장 위의 미완료 항목 1개를 고른다.
4. `5. 결정 기록`을 읽고 이미 확정된 결정을 뒤집지 않는다.
5. **`PRD.md`를 통째로 읽지 않는다.** 95KB·44장이다. 필요한 장만 잘라 읽는다.

### ▶ 작업 중 (DURING)
- 기존 파일을 통째로 다시 쓰지 않는다. 필요한 부분만 수정한다.
- 새 결정을 내렸다면 즉시 `5. 결정 기록`에 추가한다.

### ▶ 작업 종료 시 (AFTER)
1. `2. 현재 상태 스냅샷` 갱신
2. `3. 진행사항 체크리스트`에서 완료 항목 `[x]` 처리
3. `4. 작업 로그`에 실제로 만든/고친 파일 경로와 결과 기록
4. `6. 다음 작업자에게`를 완전히 새로 덮어쓴다

> **이 파일을 스크립트로 고칠 때 주의.** 절 제목을 본문에서도 언급하기 때문에,
> `index("## 6. ...")` 같은 방식으로 절을 잘라내면 위 프로토콜 안내문이 먼저 걸려
> 엉뚱한 범위가 삭제된다. 실제로 2026-08-23에 §1~§5가 통째로 지워지는 사고가 났다.
> 절 단위로 자를 때는 반드시 줄 시작에 고정해서 찾을 것.

---

## 1. 프로젝트 개요 (30초 브리핑)

| 항목 | 내용 |
|---|---|
| 이름 | card-news-maker |
| 종류 | 이미지+문구로 카드뉴스·밈을 만드는 웹 도구. 야구 결과 카드뉴스 탭 포함 |
| 기술 스택 | React + TypeScript + Konva.js + Vite (`PRD.md` 22장 추천안 그대로) |
| 백엔드 | 서버리스 함수 **1개**(KBO 경기 결과 프록시). 그 외 서버 없음 |
| 저장 | localStorage. 사용자 이미지는 서버로 전송하지 않음 |
| 수익모델 | **과제 범위 제외.** 기록만 남기고 구현하지 않음 |

## 2. 현재 상태 스냅샷

- [x] **Phase 1 — PRD 갱신 완료.** 수익모델 제외 명시, 24장에 백엔드 분리 원칙 추가,
      43장(야구 결과 카드뉴스 탭)·44장(백엔드 구조) 신설.
- [x] **Phase 2 — 화면 시안 완료.** `Home.dc.html`·`Main.dc.html`·`BaseballTab.dc.html`·
      `EditorMobile.dc.html` 4장 + `canvas.json` 아트보드 배치. 다크 워크스페이스·IBM Plex Sans KR·
      강조색 `oklch(0.76 0.152 62)` 방향으로 통일. **아직 실제 시안 캔버스 도구에 저장하고
      사용자가 최종 확인하는 절차는 남아있음(파일만 전달된 상태).**
- [x] **Phase 3 — 스캐폴딩 + 브라우저 실검증.** Vite+React+TS+Konva 설치, `src/types/`에 PRD 21·43장
      인터페이스 이식, 빈 캔버스 1080×1080 렌더링, 이미지 업로드(F-01), 텍스트 추가(F-02) 최소 구현.
      `tsc -b`·`vite build`·`oxlint` 통과. **브라우저 실검증 완료 (2026-08-23) — 통과, 콘솔 오류 0건.**
- [x] **과제 카드 1·2 — 편집·미리보기 및 화면비 일치 완료.** PNG/JPG 업로드, 텍스트 스타일/문구 편집,
      1:1 (1080×1080), 4:5 (1080×1350), 9:16 (1080×1920) 비율 전환 및 고해상도 내보내기 100% 일치 실검증 통과.
- [x] **과제 카드 4·5 — 템플릿 CRUD와 JSON 이동·방어 완료.** 기본 템플릿 3종, 현재 캔버스 저장,
      불러오기, 이름 수정, 삭제를 `card-news-templates` localStorage에 영속화. F5 새로고침 유지,
      전체 JSON 내보내기/가져오기, 문법 오류·필수 필드 누락 방어 및 기존 데이터 보존 실검증 통과 (콘솔 오류 0건).
- [x] **레이어 패널 UI (F-03-2) 및 사이드바 탭 통합 완료.** 레이어 선택, 표시(눈)/잠금(자물쇠) 토글,
      Z-Index 순서 위/아래 이동, 삭제 및 템플릿/레이어 탭 네비게이션 연동.
- [x] **모바일 반응형 편집 UX 완료 — Claude 브라우저 실검증 통과 (2026-08-24).** `EditorMobile.dc.html`의
      텍스트 속성 바텀시트(peek/expand), 하단 고정 삽입 툴바, 레이어/템플릿 패널 아이콘 토글 오버레이를
      900px 이하에 이식. 900px 초과 데스크톱 레이아웃과 기존 편집·내보내기 동작은 유지.
      Chrome DevTools MCP로 390×844 실기기 뷰포트에서 직접 조작해 확인, 콘솔 오류 0건.
- [x] **Phase 4 — 야구 탭 + KBO 프록시 + 구단 엠블럼 수집 완료 (Claude, 2026-08-24).**
      실제 KBO 공식 웹서비스(`ws/Schedule.asmx/GetScheduleList`)를 조사해 크롤링·정규화 로직 구현,
      구단 엠블럼 10종 실제 파일 수집, F-30~F-33 전부 구현·브라우저 실검증 통과. 상세는 §4 참고.
- [x] **Phase 5 — 배포 완료 (사용자, 2026-08-24).** https://card-news-maker-beige.vercel.app/
      배포 직후 `/api/kbo-schedule`가 500(FUNCTION_INVOCATION_FAILED)이던 것을 Claude가 발견·수정.
      수정 커밋 `41cb9db` 반영 후 `/api/kbo-schedule?date=2026-08-01`의 정상 JSON 응답까지 확인했다.
- [x] **구단 엠블럼 최종 정리 완료 (Claude·Codex·agy, 2026-08-25).** 사용자 다운로드 원본과 공식
      사이트 이미지를 기반으로 10개 구단을 투명 PNG로 교체하고 팀별 실제 종횡비를 메타데이터에 기록했다.
      배지 제거 후 큰 워터마크 중심의 단순 레이아웃으로 조정했으며, KIA vs SSG·LG vs 삼성에서
      종횡비 오차 0.00%와 선명한 렌더링을 브라우저로 확인했다. 상세 출처는 `public/logos/SOURCES.md`.
- [x] **최종 리뷰·제출 자료 완료 (Claude, 2026-08-25).** 코드리뷰 결함 4건과 오른쪽 워터마크
      z-index 결함을 수정하고, 증거 이미지 15종을 포함한 32장 PPTX/PDF를 생성했다.
- [x] **Git 반영 완료.** Claude·Codex·agy 누적 변경과 제출 산출물이 `c75185d`까지 `origin/main`에
      푸시됐으며, 2026-08-25 문서 점검 시 로컬 `main`과 `origin/main`이 일치하고 작업 트리가 깨끗했다.

## 3. 진행사항 체크리스트

- [x] PRD 40장에 수익모델 과제 범위 제외 명시
- [x] PRD 24장 7번 — 백엔드는 외부 데이터 조회 용도로만
- [x] PRD 43장 — 야구 결과 카드뉴스 탭 (F-30~F-33, 화면 설계, 데이터 구조, 로고 정책)
- [x] PRD 44장 — 백엔드 구조 (API 명세, 캐싱, 실패 처리, 보안)
- [x] `CLAUDE.md` 작성
- [x] `design/Home.dc.html` — 홈 화면 시안
- [x] `design/Main.dc.html` — 편집기 PC 시안
- [x] `design/BaseballTab.dc.html` — 야구 탭 시안
- [x] `design/EditorMobile.dc.html` — 편집기 모바일 시안
- [x] `design/canvas.json` — 아트보드 배치
- [ ] 시안 캔버스로 저장 후 사용자 확인 — **파일은 준비됐으나 이 저장·확인 절차는 아직 미완료**
- [x] Vite 프로젝트 초기화 + Konva 설치
- [x] `src/types/` — PRD 21장·43장 인터페이스 이식
- [x] 빈 캔버스 1080×1080 렌더링
- [x] 이미지 업로드(F-01) 구현
- [x] 이미지 업로드 브라우저 실검증 — 통과 (2026-08-23)
- [x] 텍스트 추가(F-02) 및 텍스트 내용 직접 편집
- [x] PNG 다운로드 버튼 연결(F-05-1)
- [x] PNG/JPG 다운로드(F-05-1, F-05-2) — JPG 품질 90%, `card-news-YYYYMMDD_HHmmss` 파일명
- [x] 1:1 (1080×1080), 4:5 (1080×1350), 9:16 (1080×1920) 비율 전환 및 정확한 해상도 내보내기 (과제 카드 2)
- [x] 이미지·텍스트 위치 이동 및 변형(F-01-4, F-01-5, F-02-3) — 선택, 드래그, 크기 조절, 회전
- [x] 텍스트 스타일 패널 연결(F-02-5~F-02-11) — 글꼴, 크기, 색상, 정렬, 굵기, 기울임, 외곽선
- [x] 템플릿 생성·불러오기·이름 수정·삭제 — localStorage 새로고침 유지 (과제 카드 4)
- [x] 기본 템플릿 3종 제공 및 최초 실행 시 저장
- [x] 전체 템플릿 JSON 내보내기·가져오기 (과제 카드 5)
- [x] 깨진 JSON 문법·필수 필드 누락 방어 검사 — 실패 시 기존 템플릿 보존
- [x] 레이어 패널 UI(F-03-2) — 가시성·잠금 토글, Z-Index 이동, 삭제, 사이드바 탭 통합
- [x] 모바일 반응형 레이아웃 — 바텀시트, 하단 고정 툴바, 레이어 패널 아이콘 토글
- [x] `GET /api/kbo-schedule?date=YYYY-MM-DD` 백엔드 프록시 — 실제 KBO 크롤링·정규화 (과제 4)
- [x] 구단 엠블럼 10종 실제 파일 수집 + `public/logos/SOURCES.md` 출처 기록
- [x] F-30 탭 전환 (일반 카드/야구 카드 소스만 전환, 편집기·다운로드는 공통)
- [x] F-31 경기 목록 조회 (날짜 입력 → 조회 → 상태별 배지 목록)
- [x] F-32 스코어보드 카드 자동 생성 (양팀 상징색 배경·엠블럼·점수·WIN 배지·날짜/구장)
- [x] F-33 경기 상태 안내 (종료/취소/예정/경기없음/조회실패 5가지 모두 처리, 예정 경기는 카드 생성 버튼 비활성화)
- [x] 조회 실패 시 팀·점수 직접 입력 폴백 (PRD 44장)
- [x] 배포된 `/api/kbo-schedule` 재배포 후 정상 응답 재확인 (41cb9db 반영 확인됨, 2026-08-25)
- [x] 구단 엠블럼 10종을 투명 PNG로 최종 교체하고 팀별 실제 종횡비 적용 (완료, 2026-08-25)
- [x] 웹폰트(IBM Plex Sans KR) 실제 로드 결함 수정 및 브라우저 실측 검증 (2026-08-25)
- [x] 이미지 업로드 15MB 상한 디코드 성능 실측 검증 (2026-08-25)

## 4. 작업 로그

- 2026-08-23: 사용자가 기존 `meme-card-maker/`와 분리된 새 프로젝트로 진행하기로 결정.
  개발요청서를 갱신해 새 폴더로 이동:
  - 수익모델 10종은 표를 그대로 두고 "과제 제출본에서 하나도 구현하지 않는다"를 40장 말미에 명시
  - 24장에 7번 원칙 추가 — 백엔드는 CORS로 막힌 외부 데이터 조회에만 쓰고, 사용자 이미지는 이 경로를 거치지 않는다
  - 43장 신설 — 야구 결과 카드뉴스 탭. 기능 4개(F-30 탭 전환 / F-31 경기 목록 조회 /
    F-32 스코어보드 자동 생성 / F-33 경기 상태 안내), 화면 설계, `KboGame` 데이터 구조,
    구단 로고 정책, 저작권 주의사항
  - 44장 신설 — 백엔드 구조. `GET /api/kbo-schedule?date=YYYY-MM-DD` 단일 엔드포인트,
    과거 1일·오늘 10분 CDN 캐싱, 조회 실패 시 수동 입력 폴백, 환경변수 취급 원칙
  - 검증: 목차 44개 항목과 장 번호 1:1 일치, 총 1968줄
  - `card-news-maker/` 생성 후 PRD를 `PRD.md`로 이동, `CLAUDE_템플릿.md` 기반으로 `CLAUDE.md` 작성
- 2026-08-23: **Phase 2 — 화면 시안 4장 + canvas.json 작성.**
  - `Home.dc.html`: 히어로 + 예시 카드 + 템플릿 5종 미리보기(야구 배지 포함)
  - `Main.dc.html`: 편집기 PC 셸(헤더·탭바·레이어 패널·캔버스·텍스트 스타일 패널), 텍스트 레이어 선택 상태
  - `BaseballTab.dc.html`: Main과 동일 셸에서 좌측만 경기 목록(종료/우천취소 상태 배지)으로 교체,
    스코어보드 미리보기(구단 상징색 배경 + 엠블럼 placeholder + WIN 배지 + 결과 문구)
  - `EditorMobile.dc.html`: Main과 동일 데이터 상태를 모바일 레이아웃(속성 패널 바텀시트,
    하단 고정 삽입 툴바, 레이어 패널은 아이콘 토글로 접힘)으로 재구성
  - `canvas.json`: Home→Main→BaseballTab을 가로 배치(사용자 플로우 순서), EditorMobile은 Home 아래
  - 세 데스크톱 파일의 색 토큰(`oklch(0.17 0.008 264)` 배경, `oklch(0.76 0.152 62)` 강조색)이
    픽셀 단위로 일치함을 확인
- 2026-08-23: **Phase 3 — 스캐폴딩 착수.**
  - Vite react-ts 템플릿 + `konva`, `react-konva`, `use-image` 설치
  - `src/types/canvas.ts` — PRD 21장 `CanvasProject`/`Layer`/`Template`/`LicenseInfo` 등 이식
  - `src/types/baseball.ts` — PRD 43장 `CardSource`/`KboGame`/`KboTeamScore` 이식
  - `src/index.css` — Vite 기본 템플릿 스타일을 걷어내고 시안 CSS 변수(`--color-bg`,
    `--color-accent` 등)로 교체
  - `src/utils/createProject.ts` — 빈 `CanvasProject` 생성(기본 1080×1080)
  - `src/components/canvas/CanvasStage.tsx` — `ResizeObserver`로 컨테이너 크기를 재서 캔버스를
    fit 표시(PRD 14장 원칙), 레이어 없을 때 안내 문구, 레이어를 `zIndex` 순으로 그림
  - `src/components/canvas/ImageLayerNode.tsx` — `use-image`로 이미지 레이어 렌더링
  - `src/features/image/loadImageFile.ts` — F-01 검증 로직(형식/용량/손상 파일), 31장 오류
    메시지 문구 그대로 사용
  - `src/App.tsx` — 헤더 툴바 + 이미지 업로드 시 cover 방식 배치 + 텍스트 추가(F-02) 최소 구현
  - 검증: `tsc -b`, `vite build`, `oxlint` 모두 통과
- 2026-08-23: **브라우저 실검증 완료.** 이전 세션이 남긴 미검증 항목을 실제 브라우저로 확인.
  - `node_modules`가 미설치 상태였음 → `npm install`
  - 상위 `.claude/launch.json`에 `card-news-maker` 항목 추가
    (`cd card-news-maker && npm run dev`, 포트 5173 — 기존 `secure-bulletin-board` 항목과 같은 패턴)
  - 검증 결과: 빈 캔버스 1080×1080 fit 표시 정상 / 이미지 업로드 후 캔버스 표시 정상(cover) /
    `layers`에 추가되어 빈 상태 안내 문구 사라짐 / 한글 `오늘도 화이팅!` 조합·외곽선 정상 /
    미지원 형식(GIF)·손상 PNG 오류 문구가 PRD 31장 그대로 출력 / 콘솔 오류 0건
  - **결함 발견**: `src/App.tsx:121` PNG 다운로드 버튼에 `onClick`이 없어 눌러도 아무 일도 없음
  - `README.md` 정합성 복구 — "코드는 아직 없다"로 남아 있던 상태 문구와 폴더 구조를 실제 디스크 기준으로 갱신
  - **사고 기록**: 이 체크리스트를 스크립트로 일괄 갱신하다가, 절 제목 문자열이 §0 안내문에도 있는 것을
    놓쳐 §1~§5가 삭제됨. 같은 세션에서 원본을 복원했고 재발 방지 규칙을 §0 끝에 적어둠
- 2026-08-24: **Phase 3 — 핵심 편집·내보내기 구현.**
  - `src/components/canvas/CanvasStage.tsx` — Stage ref 외부 노출, 내보내기 중 Transformer 숨김,
    fit 배율 역수 `pixelRatio: 1 / scale` 적용, 레이어 선택·드래그·크기 조절·회전 및 상태 반영
  - `src/components/canvas/ImageLayerNode.tsx` — 이미지 선택과 드래그/변형 이벤트 연결
  - `src/components/editor/TextStylePanel.tsx` — 글꼴, 크기, 글자색, 정렬, 굵기, 기울임,
    외곽선 색상·두께 편집 UI 추가
  - `src/features/export/downloadCanvas.ts` — 안전한 날짜시간 파일명과 브라우저 다운로드 처리
  - `src/App.tsx`, `src/App.css` — PNG/JPG 버튼, 선택 상태, 스타일 패널 레이아웃 연결
  - 검증: `npx tsc -b`, `npm run lint`, `npm run build` 통과.
- 2026-08-24: **Phase 3 — agy 검증 및 결함 보완 완료.**
  - Headless Chrome CDP 자동화 실검증 수행:
    - 이미지/텍스트 선택, 드래그 이동 및 Transformer 변형 정상 동작 확인
    - 텍스트 스타일 패널(글꼴, 크기, 색상, 정렬, 굵기, 기울임, 외곽선) 실시간 반영 확인
    - PNG/JPG 내보내기 정상 동작 및 1080×1080 해상도 정확 일치 확인
    - Transformer 선택 테두리/핸들이 내보내기 이미지에서 제외됨을 픽셀 단위로 확인
    - JPG 헤더(`data:image/jpeg`), 품질(0.9), 한글 렌더링 정상, 콘솔 오류 0건 확인
  - 결함 보완:
    - `src/components/editor/TextStylePanel.tsx` — 텍스트 문구 내용(`text`)을 직접 수정할 수 있는 입력 필드(textarea) 추가 (F-02-1 대응)
    - `src/components/canvas/CanvasStage.tsx` — `KonvaText`에 `fillAfterStrokeEnabled={true}` 추가하여 외곽선 적용 시 글자 가독성 개선
  - 재검증: `oxlint` (0 errors, 0 warnings), `tsc -b`, `vite build` 모두 통과
- 2026-08-24: **과제 카드 4·5 — 템플릿 CRUD 및 JSON 가져오기/내보내기 구현.**
  - `src/features/template/templateStore.ts` — `card-news-templates` localStorage 저장소, 기본 템플릿
    3종, 캔버스 스냅샷 생성·복원, 버전이 있는 JSON 직렬화와 레이어별 방어 검증 구현
  - `src/components/template/TemplatePanel.tsx` — 현재 캔버스 저장, 목록 불러오기, 이름 수정,
    개별 삭제, 전체 JSON 가져오기·내보내기 UI 구현
  - `src/App.tsx`, `src/App.css` — 템플릿 상태와 원자적 저장 처리, 성공·오류 배너 및 패널 연결
  - 방어 원칙: 가져온 JSON 전체가 검증된 후에만 localStorage와 React 상태를 함께 교체하여
    문법 오류나 필수 필드 누락 시 기존 템플릿을 그대로 보존
  - 검증: 정상 JSON 왕복, 깨진 JSON, 필수 필드 누락 테스트 통과. `oxlint`, `npx tsc -b`,
    `npm run build` 통과 및 Chrome headless 초기 화면 렌더링 확인

- 2026-08-24: **과제 카드 1~5 종합 검증 및 레이어 패널/비율 전환 완성 (agy).**
  - `src/components/layer/LayerPanel.tsx` — 레이어 패널 UI 신설 (가시성·잠금 토글, Z-Index 순서 위/아래 이동, 삭제)
  - `src/App.tsx`, `src/App.css` — 좌측 사이드바 탭([레이어]/[템플릿]) 통합, 상단 1:1·4:5·9:16 비율 프리셋 전환기 연결
  - `src/components/canvas/CanvasStage.tsx` — 내보내기 시 부동소수점 오차 없는 100.0% 정확한 1080px 렌더링 수식 적용
  - Headless Chrome CDP 종합 실검증 완료:
    - **카드 2 (화면비 & 해상도):** 1:1(1080×1080), 4:5(1080×1350), 9:16(1080×1920) 픽셀 단위 일치 검증 통과 (100% 일치)
    - **카드 4 (템플릿 CRUD):** 기본 템플릿 3종 표시, 현재 캔버스 저장, 목록 불러오기, 이름 수정, 개별 삭제 및 F5 새로고침 후 localStorage 영속성 유지 통과
    - **카드 5 (JSON 이동 & 방어):** 정상 JSON 내보내기/가져오기 왕복 통과, 깨진 JSON 문법 및 필수 필드 누락 스키마 거부 + 기존 데이터 100% 보존 통과
    - **레이어 패널:** 표시 토글, 잠금 토글, Z-Index 재정렬 정상 동작 확인
    - **콘솔 오류:** 0건 (Clean)
  - 재검증: `oxlint` (0 errors), `npx tsc -b`, `vite build` 모두 완벽 통과

- 2026-08-24: **Claude — 과제 제출 문서 검토 및 Phase 4/5 방향 정리.**
  - `docs/00_과제_요구사항_매핑.md` 12~17번 항목을 실제 소스(`templateStore.ts`, `TemplatePanel.tsx`,
    `App.tsx`)와 대조해 "구현 진행 → 충족"으로 갱신, §5(재검증 결과·모바일 격차 분석·Phase 4/5 제안) 신설
  - 모바일 반응형 격차 분석: `design/EditorMobile.dc.html`이 요구하는 바텀시트·하단 고정 툴바·
    레이어 패널 아이콘 토글 3가지가 현재 `src/App.css`의 단순 세로 스택(`@media (max-width: 900px)`)에는
    없음을 확인. 기능은 깨지지 않으나 시안 의도의 모바일 UX는 미구현 상태로 §2·§3에 반영
  - 재검증: `npx tsc -b`, `npm run lint`(oxlint), `npm run build` 모두 통과 (신규 결함 없음)
  - 결정: Phase 4(야구 탭)는 20개 채점 항목에 포함되지 않는 보너스 범위이므로 제출 마감 이후로 순연,
    Phase 5(배포)는 Vercel 커넥터 미인증으로 사용자의 대화형 인증이 선행되어야 함 (§5 결정 기록 참고)
- 2026-08-24: **Codex — 모바일 편집 UX 시안 이식.**
  - `src/App.tsx` — 모바일 레이어/템플릿 패널 열림 상태와 텍스트 속성 바텀시트 확장 상태 추가,
    레이어·속성 토글 및 이미지·텍스트 삽입을 하단 고정 툴바에 연결
  - `src/App.css` — 900px 이하에서 좌측 패널을 캔버스 위 오버레이로 전환하고 배경 닫기 버튼 추가,
    텍스트 속성 패널을 58px peek/최대 58vh 바텀시트로 전환, 66px 하단 툴바와 safe-area 처리
  - 모바일 헤더는 비율 선택과 PNG 저장만 유지하고 삽입 액션은 하단으로 이동
  - 검증: `npx tsc -b`, `npm run lint`, `npm run build` 통과. Chrome headless 1600×900
    데스크톱 및 390×844 모바일 초기 화면 비교에서 PC 레이아웃 회귀 없음과 모바일 패턴 표시 확인

- 2026-08-24: **Claude — 모바일 UX 브라우저 실검증 (사용자가 "안티그래비티 작업 완료" 보고 후 확인 요청).**
  - Chrome DevTools MCP로 390×844(모바일 뷰포트) 조작 검증:
    - 초기 화면에서 헤더(비율 선택·저장 버튼), 캔버스, 텍스트 스타일 바텀시트 peek, 하단 고정
      툴바(이미지/텍스트/레이어/속성) 모두 시안대로 렌더링됨을 스크린샷으로 확인
    - 하단 툴바의 "레이어" 토글로 사이드바 오버레이 열기/닫기 정상 동작 확인 (a11y 스냅샷 `expanded` 상태 전환)
    - "텍스트" 버튼으로 레이어 추가 → 레이어 목록에 즉시 반영, 속성 바텀시트가 선택된 레이어 요약으로 갱신됨을 확인
    - 속성 peek 클릭 시 바텀시트가 58px → 최대 58vh로 확장되어 글꼴·크기·색상·정렬·외곽선 컨트롤이
      스크롤 가능한 전체 패널로 펼쳐짐을 확인
    - 콘솔 오류 0건
  - 1440×900(데스크톱) 재확인: 좌측 고정 사이드바 + 우측 고정 텍스트 스타일 패널 레이아웃 회귀 없음, 콘솔 오류 0건
  - `npx tsc -b`, `npm run lint`(oxlint), `npm run build` 재확인 — 모두 통과
  - **경미한 관찰 사항(결함 아님)**: `.mobile-sidebar-backdrop`는 화면 전체를 덮지만 그 위에 `.left-sidebar`
    (레이어/템플릿 패널, `z-index: 30`)가 겹쳐 있어, 실제로 배경을 탭해 닫을 수 있는 영역이 패널
    오른쪽의 좁은 여백(약 40px)과 위/아래 가장자리로 제한된다. 하단 툴바의 "레이어" 버튼을 다시 눌러
    닫는 주 동선은 완전히 정상 동작하므로 기능 결함은 아니나, 실사용성 개선이 필요하면 배경 탭 시
    항상 닫히도록 손볼 수 있다(제출 채점 항목에는 없음)
  - 결론: 모바일 반응형(선택 항목)까지 포함해 과제 관련 작업이 모두 검증됨. 남은 것은 Phase 4/5
    우선순위에 대한 사용자 선택뿐

- 2026-08-24: **Claude — Phase 4(야구 탭) 전체 구현.** 사용자가 "배포는 직접 하겠다, 페이즈 4로
  해달라"고 요청. 실제 크롤링 소스 조사와 구단 엠블럼 실제 수집까지 진행하기로 범위를 확정한 뒤 구현.
  - **조사**: KBO 공식 사이트(`koreabaseball.com`)가 자체 일정 화면에서 쓰는 비공개 웹서비스
    `POST /ws/Schedule.asmx/GetScheduleList`를 실제 요청으로 검증. 월 단위 조회, 응답은 HTML이
    섞인 JSON. 종료/취소/예정 경기의 마크업 차이(`class="win/lose/same"`, 취소사유 칼럼 텍스트)를
    2026년 8월 실데이터로 전부 확인
  - `api/_kboClient.ts` — 위 웹서비스 호출·파싱·`KboGame[]` 정규화, 팀명→코드 매핑(SK→SSG,
    HT→KIA, OB→두산 등 과거 코드 승계 포함), 날짜 형식·조회 실패 오류 클래스 분리
  - `api/kbo-schedule.ts` — Vercel 서버리스 함수(`@vercel/node` 의존성 없이 표준 Node http 타입만
    사용, 배포 시 그대로 동작). `tsconfig.api.json` 신설해 `tsc -b`에 포함
  - `vite.config.ts` — 같은 핸들러를 로컬 dev 미들웨어로 재사용해 `vercel dev` 없이 `npm run dev`
    만으로 `/api/kbo-schedule`을 검증 가능하게 함
  - `public/logos/*.png` — KBO 공식 CDN(`emblem/regular/fixed/emblem_<코드>.png`)에서 10개 구단
    엠블럼 실제 다운로드(64×41px), `public/logos/SOURCES.md`에 출처·취득일·해상도 한계 기록
  - `src/features/baseball/teams.ts` — 구단 코드별 상징색·엠블럼 경로
  - `src/features/baseball/fetchKboSchedule.ts` — 프록시 호출 클라이언트 래퍼
  - `src/features/baseball/buildScoreboardLayers.ts` — F-32 스코어보드 레이어 생성(양팀 배경 분할
    도형, 엠블럼, 팀명, 점수/취소문구, WIN 배지, 날짜·구장)
  - `src/components/baseball/BaseballTab.tsx` — F-30~F-33 UI, 조회 실패 시 팀·점수 직접 입력
    폴백(PRD 44장)
  - `src/App.tsx` — 사이드바 탭에 "야구" 추가(레이어/템플릿/야구 3-way), 카드 생성 시 프로젝트
    레이어·배경색 교체 후 레이어 탭으로 자동 전환
  - **결함 발견 및 수정**: 브라우저 실검증 중 스코어보드 배경이 단색으로만 보임을 발견 —
    `CanvasStage.tsx`가 애초부터 `ShapeLayer`(도형) 타입을 렌더링하지 않던 기존 결함이었음
    (템플릿 검증 로직에는 있었지만 캔버스 렌더러에 rect/circle 케이스가 없었음). `Rect`/`Circle`
    렌더링을 추가해 근본 수정 — 이제 도형 레이어는 어디서든 정상 동작
  - **재발견 결함**: 도형 렌더링 추가 중 React key를 spread 객체 안에 넣어 콘솔 경고 발생 → key를
    JSX 속성으로 직접 분리해 수정
  - Chrome DevTools MCP 실검증: 종료 경기(SSG 10:2 키움, WIN 배지 포함), 취소 경기(삼성 vs 롯데
    폭염취소), 경기 없음(2026-08-24 평일 휴식일), 조회 실패(잘못된 날짜) 전부 확인. 내보내기
    캔버스의 실제 픽셀(좌/우 배경색)을 `getImageData`로 채취해 화면과 파일이 일치함을 확인.
    390×844 모바일에서도 사이드바 오버레이 안에서 3-way 탭과 야구 조회 정상 동작 확인. 콘솔 오류 0건
  - 재검증: `npx tsc -b`, `npm run lint`(oxlint), `npm run build` 모두 통과
  - `README.md` — 상태 문구·폴더 구조·저작권 출처(구단 개별 CI → KBO 공식 사이트로 정정) 갱신

- 2026-08-24: **Claude — 배포 직후 `/api/kbo-schedule` 500 오류 진단·수정.** 사용자가
  https://card-news-maker-beige.vercel.app/ 배포 완료를 알려 확인하던 중, 메인 페이지는 200이지만
  `/api/kbo-schedule`가 잘못된 날짜 요청에서도(즉 KBO 네트워크 호출 전 단계에서부터) 500
  `FUNCTION_INVOCATION_FAILED`를 반환함을 발견.
  - **원인**: `api/kbo-schedule.ts`가 `from "./_kboClient"`처럼 확장자 없이 상대 경로를 import했다.
    Vite의 dev 서버 리졸버(`server.ssrLoadModule`)는 확장자 없는 import를 허용해 로컬에서는 문제가
    드러나지 않았지만, Vercel이 실제로 실행하는 컴파일된 Node ESM 런타임은 상대 import에 확장자를
    강제한다 — 즉 **로컬 dev 검증(§4 Phase 4 로그)이 이 결함을 잡아내지 못하는 구조적 사각지대**였다.
  - **재현**: `node -e "import('./api/kbo-schedule.ts')"`로 로컬에서 동일한
    `Cannot find module './_kboClient'` 오류를 재현. `tsc`로 Vercel과 동일하게 TS→JS 컴파일한 뒤
    컴파일된 산출물을 직접 `node`로 실행해 원인을 확정
  - **수정**: `api/kbo-schedule.ts`의 import를 `from "./_kboClient.js"`로 변경(컴파일 후 실제 파일명과
    일치). `_kboClient.ts`가 `src/types/baseball`을 가져오는 import는 `import type`이라 컴파일 시
    완전히 지워지므로 수정 불필요함을 확인
  - **검증**: 컴파일된 산출물을 mock request/response로 직접 호출해 실제 KBO 데이터가 200으로
    정상 반환됨을 확인. `npx tsc -b`·`npm run lint`·`npm run build` 재통과, `npm run dev`로 로컬
    프록시도 재확인(회귀 없음)
  - `.gitignore`에 `*.stackdump` 추가 — 이 세션에서 Git Bash가 반복적으로 크래시 덤프 파일을
    저장소 루트에 남겨 매번 수동으로 지워야 했던 문제를 방지
  - **다음 필요 조치**: 이 수정 커밋을 Vercel에 재배포해야 실제 서비스가 정상화된다(§6 참고)

- 2026-08-24: **Claude — 구단 엠블럼 저해상도 문제 진단 (작업 도중 사용자가 중단·인계 지시).**
  - 사용자가 배포본에서 KIA 카드를 만들어보고 "구단로고가 안보이네"라고 보고, 이어서
    "로고의 크기가 작아서 그런가"라고 추가 언급
  - Chrome DevTools MCP로 로컬 dev 서버에서 재현: 실제로는 렌더링되고 있으나(레이어 목록에 이미지로
    존재, 캔버스에도 존재) `public/logos/HT.png`가 64×41px라 110×70px로 확대하면 검은 얼룩처럼
    보여 사실상 식별 불가능함을 스크린샷으로 확인. **기능 결함이 아니라 자산 해상도 문제**로 확정
  - 사용자가 `C:\Users\stuls\Downloads\10개구단 로고ci.jpg` (600×586px, 5행×2열 그리드, 행마다
    다른 배경색 타일 안에 각 구단 풀컬러 로고가 큼직하게 배치된 이미지)를 제공하며 이 이미지에서
    로고를 추출해 교체해달라고 요청
  - **격자 배치(이미지 좌상단 기준, 왼쪽 열 → 오른쪽 열, 위 → 아래)**:
    1행: 두산 베어스(OB) / KIA 타이거즈(HT)
    2행: 롯데 자이언츠(LT) / KT wiz(KT)
    3행: 삼성 라이온즈(SS) / LG 트윈스(LG)
    4행: 키움 히어로즈(WO) / NC 다이노스(NC)
    5행: 한화 이글스(HH) / SSG 랜더스(SK)
  - **경계 분석(Pillow로 좌측 y=5 세로선을 스캔해 배경색 전환 지점 확인)**: 행 경계가 y≈0, 118, 236,
    354, (분석 중단됨 — 471 근처로 추정), 586에서 나타남. 600÷5=117.2px 균등 분할과 거의 일치해
    "각 행 높이 ≈117.2px, 열 폭 300px(600÷2)"로 균등 크롭해도 안전할 것으로 보이나, **열(세로)
    경계는 가로 스캔을 시작하다가 중단되어 검증되지 않았다** — 다음 작업자가 마저 확인할 것
  - **미해결 설계 판단**: 이미지의 각 타일은 로고만 있는 게 아니라 구단 배경색·사선 장식이 함께
    있는 디자인이다. (a) 타일 전체를 그대로 크롭해 쓸지, (b) 로고 배지 부분만 타이트하게 잘라내
    배경을 투명하게 만들지 결정이 안 됐다. `buildScoreboardLayers.ts`가 이미 자체적으로 팀 상징색
    배경을 깔기 때문에, 타일 전체를 쓰면 배경이 이중으로 겹쳐 보일 수 있어 (b)가 더 나을 가능성이
    높다고 판단했으나 확정하지 못하고 중단됨
  - **여기서 작업 중단.** 어떤 프로젝트 파일도 아직 고치지 않았다(`git status` clean) —
    이미지 분석만 하고 실제 크롭·교체·커밋은 진행하지 못했다
  - 사용자가 추가로 "MVP 선수 사진을 크롤링해서 쓰고 싶다"는 아이디어를 제시했으나 **"꼭 추가하지
    않아도 괜찮은 제안"이라고 명시**했다. Claude는 이 자리에서 다음 의견만 남기고 구현하지 않았다:
    KBO 일정 API에는 경기별 MVP 정보가 없어 별도 페이지(게임센터/박스스코어)를 추가로 크롤링해야
    하고, 선수 개인 초상권은 구단 로고보다 훨씬 민감한 문제라 신중히 검토가 필요하다는 점.

- 2026-08-25: **Claude — 야구 스코어보드 로고 배지 어색함 수정 (사용자 피드백).** 사용자가 배포본에서
  KIA vs 키움 카드를 만들어보고 "기아 로고가 이상하지 않아?"라고 지적, 이어서 "원본이 이상한 건
  아니야, 만들어져 있는 카드뉴스가 어색해서 그래. 로고가 꽉차거나 해당 경기 각팀 MVP 선수 사진이
  들어갔으면 해"라고 구체화.
  - **원인 진단**: `public/logos/HT.png` 원본 파일 자체는 정상(선명한 KIA TIGERS 로고). 문제는
    카드에 얹었을 때였다 — 로고 타일은 빨강→검정 그라디언트+대각선 장식 배경인데, `teams.ts`의
    `HT.primaryColor`(`#ea0029`, 단색)로 채운 스코어보드 배경 위에 얹으니 로고 주변에 색이 어긋나는
    사각 박스가 도드라져 보였다. 또한 로고 크기가 110×44px(카드 폭의 10%)로 작아 존재감도 부족했다.
  - **수정 방향 결정**: 사용자가 대안으로 제시한 "해당 경기 각팀 MVP 선수 사진"은 보류하고(아래
    별도 항목), 더 안전하고 즉시 가능한 "로고를 크고 깔끔하게" 쪽을 먼저 적용.
  - `src/types/canvas.ts` — `ShapeLayer`에 선택적 `cornerRadius?: number` 필드 추가(하위 호환,
    기존 템플릿 JSON 영향 없음)
  - `src/components/canvas/CanvasStage.tsx` — 도형 렌더러의 `Rect`에 `cornerRadius` prop 연결
  - `src/features/baseball/buildScoreboardLayers.ts` — 로고 크기를 110px→최대 220px(카드 폭의
    14%→28%)로 확대. 로고 뒤에 흰색 둥근 배지 플레이트(`shapeLayer`, zIndex 1.9/2.9로 배경과 로고
    사이에 삽입)를 추가해 어떤 팀 색 배경 위에서도 로고가 깔끔한 뱃지로 보이도록 함. 배지가 커진
    만큼 `nameY`(팀명 위치) 계산도 배지 하단 기준으로 보정
  - 검증: `npx tsc -b`·`npm run lint`(oxlint)·`npm run build` 모두 통과. 브라우저에서 2026-08-22
    KIA vs 키움 카드를 실제로 생성해 레이어 패널로 "원정팀 엠블럼 배지"(273×150)가 "원정팀
    엠블럼"(220×88)을 정확히 감싸는 크기로 만들어짐을 확인. **단, 이 세션은 Browser 창이 실제
    화면에 표시되지 않는 상태(`document.visibilityState === "hidden"`)라 스크린샷/픽셀 비교로 눈으로
    보는 실검증은 하지 못했다** — 다음 작업자(또는 사용자)가 `npm run dev`로 직접 열어 눈으로
    확인해야 한다.
  - **보류된 제안 — MVP 선수 사진 배경**: 사용자가 제시했으나 다음 이유로 바로 착수하지 않고
    사용자 확인을 기다리는 중: (1) §5 기존 결정("가져오는 값은 경기 결과로 한정, 기사·사진 등
    창작물은 가져오지 않는다")과 정면으로 상충, (2) 현재 쓰는 KBO 일정 API에는 경기별 MVP 정보가
    아예 없어 게임센터/박스스코어 등 별도 페이지를 새로 조사해야 함, (3) 선수 개인 초상권은 구단
    로고보다 훨씬 민감함. 착수 여부는 사용자 결정 대기.

- 2026-08-25: **Claude — MVP 선수 사진 기능 조사·구현·되돌리기, 로고 워터마크 추가.** 위 항목에서
  보류했던 MVP 사진을 사용자가 "바로 구현 진행"으로 확정해 실제 착수. 상세 조사 경위는 §5의
  관련 결정 기록 참고. 요약하면:
  - Chrome DevTools MCP로 KBO 게임센터를 직접 조작해 네트워크 요청을 가로채 비공개 "키플레이어"
    API(`GetKeyPlayerHitter`/`GetKeyPlayerPitcher`, WPA 랭킹 + 선수 사진)를 찾아 실제 구현
    (`api/_kboKeyPlayerClient.ts`, `api/kbo-keyplayer.ts`, `src/features/baseball/fetchKeyPlayers.ts`)
  - 사용자가 고화질 사진 출처로 구단 공식 사이트(tigers.co.kr)를 제안 → 브라우저로는 440×364px
    투명 배경 고화질 사진 확인됐으나, **서버 사이드(`fetch`/`curl`)로는 SPA 셸만 오거나 내부 API가
    타임아웃(WAF로 추정)** — 기술적으로 자동화 불가 확정
  - 사용자가 뉴스 기사 사진도 제안 → gameId와 연결된 구조 데이터가 아니라 자동 매칭 불가,
    언론사 저작권 문제도 더 커서 채택 안 함
  - 결국 KBO 자체 91×91px 사진으로 원형 배지(팀별 1명, 이름 캡션)를 구현하고 `tsc -b`·`lint` 통과,
    브라우저에서 실제 13개 레이어(배지 2개 포함) 생성까지 확인
  - **사용자가 "중지하고 사진 사용하는 건 포기할게"라고 최종 결정** → 관련 파일 전부 삭제/원복:
    `api/_kboKeyPlayerClient.ts`, `api/kbo-keyplayer.ts`, `src/features/baseball/fetchKeyPlayers.ts`
    삭제, `buildScoreboardLayers.ts`·`BaseballTab.tsx`·`vite.config.ts`·`src/types/baseball.ts`를
    사진 기능 이전 상태로 복원. 로고 배지 수정(위 항목)은 이 되돌리기 대상이 아니라 그대로 유지
  - 되돌리지 않고 남긴 부산물: `ImageLayerNode.tsx`의 cover-fit crop(2026-08-23 결정이 원래
    요구했으나 미구현이었던 부분), `ShapeLayer.cornerRadius`
  - 곧이어 사용자가 "로고를 워터마크 형식으로 좀 크게 적용하고 싶어"라고 요청 → `src/types/canvas.ts`
    `BaseLayer`에 `opacity?: number` 신설, `CanvasStage.tsx`·`ImageLayerNode.tsx`의 모든 Konva
    노드(Rect/Circle/Text/Image)에 연결. `buildScoreboardLayers.ts`에 각 팀 배경 폭 85%짜리 큰 로고
    워터마크를 배경 바로 위 레이어로 추가(zIndex 0.5/0.6)
  - **실제 눈으로 확인함**: 이 세션은 Browser 창이 화면에 표시되지 않아(`document.visibilityState
    === "hidden"`) `computer.screenshot`이 계속 타임아웃됐다. 우회책으로 브라우저 안에서
    `window.Konva.stages[0].toDataURL()`을 직접 호출해(내보내기 버튼이 쓰는 것과 동일한 방식)
    PNG를 뽑아 로컬 파일로 저장한 뒤 Read 도구로 실제로 봤다. 결과: 로고 배지는 깔끔했지만 **워터마크
    불투명도 0.16에서 KIA(HT) 쪽 로고 타일의 어두운 그라디언트 배경이 사각형 경계로 비쳐 보임을
    확인**(§5 로고 배지 문제와 동일 원인 — 타일 배경이 단색이 아니라 팀 배경색과 안 맞음). 사용자
    피드백을 받을 필요 없이 이 세션에서 즉시 불투명도를 0.16→0.08로 낮추고 재검증해 경계가 눈에 띄게
    옅어짐을 다시 스크린샷으로 확인, 사용자에게 최종 이미지를 전달함
  - 재검증: `npx tsc -b`·`npm run lint`(oxlint) 모두 통과. 브라우저에서 2026-08-22 KIA vs 키움
    카드를 다시 생성해 레이어 패널로 13개 레이어(사진 배지 없음, 워터마크 459×184 2개 포함) 확인,
    새로 발생한 콘솔 오류 없음(하드 리로드 전후 오류 목록 동일 — 편집 중 HMR 잔여 기록으로 판단)

- 2026-08-25: **Claude — 인계받은 할 일 1·2 완료.**
  - **할 일 1 (배포 재확인)**: WebFetch로 `https://card-news-maker-beige.vercel.app/api/kbo-schedule?date=2026-08-01`
    확인 — `FUNCTION_INVOCATION_FAILED` 500이 아니라 정상 JSON 배열(경기 5건) 반환 확인. 커밋
    `41cb9db`의 수정이 이미 반영돼 있었다. 추가 조치 불필요.
  - **할 일 2 (구단 엠블럼 교체)**: `C:\Users\stuls\Downloads\10개구단 로고ci.jpg`를 Pillow로 분석.
    좌측(x=5) 세로선 배경색 전환 지점을 스캔해 행 경계 `y=0,118,236,354,471,586`을 확정하고,
    두 개 행(y=60, y=300)에서 가로 스캔해 열 경계가 `x≈294`(≈600÷2)임을 재검증(§4 2026-08-24 로그가
    "검증 못함"으로 남겼던 부분). 10개 타일을 그대로 잘라(추가 변형 없음) 320×128px로 통일
    리사이즈해 미리보기 콘택트시트로 10개 전부 육안 확인 — 그리드 경계가 정확해 이웃 타일 침범이나
    로고 잘림 없음을 확인.
  - **결정**: 이전 로그(§4 2026-08-24)가 "로고 배지만 투명 배경으로 타이트하게 자르는 편을 권장"했으나,
    실제로 비교해보니 각 타일 배경색이 `teams.ts`의 `primaryColor`와 거의 일치해(예: HH 주황, HT 빨강,
    SS 파랑) 스코어보드의 팀 상징색 배경 위에 타일을 그대로 얹어도 이중 배경 위화감이 없었다.
    투명 배경 추출은 타일 배경이 단색이 아니라 그라디언트·대각선 장식이 섞여 있어 자동 크로마키가
    부정확할 위험이 컸기 때문에, 타일 전체를 쓰는 더 단순하고 안전한 방식을 최종 채택했다(§5에 결정
    추가).
  - `src/features/baseball/buildScoreboardLayers.ts` — 로고 높이 비율을 `41/64`(옛 64×41px 기준)에서
    `128/320`(새 320×128px 기준)으로 수정. `ImageLayerNode.tsx`가 `KonvaImage`에 고정 width/height를
    그대로 넘겨(object-fit 없이) 늘려 그리므로, 비율을 맞추지 않으면 새 로고가 찌그러진다.
  - `public/logos/*.png` 10개 파일 교체, `public/logos/SOURCES.md`를 새 출처(사용자 제공 이미지,
    추출 방법, 해상도)로 갱신하고 기존 KBO CDN 출처는 "이전 출처" 절로 보존.
  - 검증: `npm run dev`(포트 5174, 기존 5173 세션과 충돌 회피) 실행 후 Chrome DevTools MCP로 야구
    탭에서 2026-08-01 KIA vs NC(우천취소) 카드를 실제로 생성 — KIA·NC 로고 모두 선명하게 표시됨을
    스크린샷으로 확인(이 문제를 처음 보고했던 KIA 카드 케이스와 동일). 콘솔 오류 0건.
    `npx tsc -b`·`npm run lint`(oxlint)·`npm run build` 모두 통과(신규 결함 없음). 개발 서버 종료.
  - **커밋·푸시는 하지 않음** — §6 프로토콜에 따라 사용자 확인 후 진행 예정.

- 2026-08-25: **Codex — 다운로드 폴더 후속 작업 완료: 10개 구단 투명 엠블럼 정리 및 비율 보존.**
  - 다운로드 폴더를 확인해 Claude가 사용자 다운로드 원본 8개를 이미 `public/logos`에 변환·반영한
    상태임을 확인했다. LG·삼성만 이전 320×128 배경 타일로 남아 있었다.
  - LG 트윈스 공식 사이트가 일정 화면에서 제공하는 투명 팀 이미지에서 LG(120×120)와
    삼성(480×480) 엠블럼을 확보해 `LG.png`, `SS.png`를 교체했다.
  - 새 10개 파일은 비율이 서로 다른데 스코어보드가 옛 320×128 비율을 강제해 일부 엠블럼을
    자르거나 찌그러뜨릴 수 있던 결함을 발견했다. `teams.ts`에 원본 비율을 기록하고
    `buildScoreboardLayers.ts`가 공통 최대 영역 안에서 contain 크기를 계산하도록 수정했다.
  - `public/logos/SOURCES.md`를 실제 최종 파일·출처·변환 이력 기준으로 갱신했다.
  - 검증: `npx tsc -b`, `npm run lint`, `npm run build` 모두 통과. Vite의 500kB 초과 청크 경고는
    기존 비차단 경고이며 빌드는 성공했다. 커밋·푸시는 사용자 승인 전이라 실행하지 않았다.

- 2026-08-25: **agy — 구단별 비율 보존 및 배지/워터마크 렌더링 실검증 완료.**
  (원래 §5에 잘못 들어가 있던 항목을 2026-08-25 Claude 검토 때 §4로 옮김. 여기서 검증한 "흰색 배지
  플레이트"는 이후 Codex의 레이아웃 단순화 커밋에서 제거되어 현재는 워터마크만 남아 있다.)
  - Headless Chrome CDP 자동화를 통해 야구 탭에서 종횡비 차이가 큰 경기 카드를 실제 생성 및 픽셀/수치 검증:
    - **KIA (HT, 종횡비 1.26) vs SSG (SK, 종횡비 1.70)**: 흰색 배지 플레이트 내부 contain 크기 계산(190.6×151.2, 220×129.7) 및 배경 워터마크(459×364.1, 459×270.6) 모두 원본 종횡비 대비 오차 `0.00%`로 잘림·찌그러짐 없이 완벽 렌더링 확인.
    - **LG (종횡비 1.00) vs 삼성 (SS, 종횡비 1.00)**: 1:1 정사각 엠블럼 배지 및 워터마크(453.6×453.6) 모두 종횡비 오차 `0.00%`로 완벽 렌더링 확인.
    - 점수 입력 및 WIN 배지 렌더링, 날짜·구장 표기 정상 확인.
  - 빌드 및 정적 분석 재검증: `npx tsc -b`, `npm run lint`(oxlint 0 errors, 0 warnings), `npm run build` 모두 완벽 통과.

- 2026-08-25: **Claude — Codex 작업물 코드리뷰 및 결함 4건 수정.** 사용자가 "codex가 작업했어,
  플러그인과 스킬을 사용해서 확인해봐"라고 요청해 `/code-review` 스킬로 커밋 5개(`3608d72..aa0d0c8`)를 검토.
  - **검토 결과 확인된 것**: 팀별 `logoAspectRatio` 10개가 실제 PNG 크기와 전부 일치, `tsc -b`·
    `oxlint`·`vite build` 모두 통과. Codex가 LG·삼성 로고까지 채워 10개 구단이 완성됨.
  - **수정 1 — `public/logos/SOURCES.md` 출처 뒤바뀜**: KT.png와 OB.png의 원본 파일이 서로
    바뀌어 적혀 있었다. 실제로는 KT.png(267×283)가 `InitialSymbol_ai.ai`(kt wiz K 방패),
    OB.png(244×244)가 `img_emblem_2025_1.webp`(두산 베어스 256×256)에서 나왔다. 저작권 출처
    기록 파일이라 제출물 정합성에 직접 영향이 있어 바로잡았다.
  - **수정 2 — 가져온 템플릿 JSON의 검증 구멍**: `BaseLayer.globalCompositeOperation`(자유 문자열,
    `as any` 캐스팅, 설정하는 코드 0곳)과 `opacity`가 `templateStore`의 가져오기 검증을 통과했다.
    전자는 `"destination-out"` 하나로 아래 레이어를 지울 수 있어 필드를 제거했고(§5 결정),
    후자는 `hasValidOptionalOpacity()`로 "없으면 통과, 있으면 0~1 유한수"만 허용하도록 했다.
    검증 케이스 7종(미지정/정상/0/-1/5/문자열/NaN)을 실제로 돌려 기대대로 통과·거부됨을 확인.
  - **수정 3 — LG 로고 저해상도**: LG.png만 120×120이라 워터마크(513px)로 약 4.3배 확대되며
    혼자 뭉개졌다. 위키백과의 LG 트윈스 로고 **SVG**를 1280px PNG로 렌더하고 투명 여백을 트림해
    1174×920으로 교체, `teams.ts`의 `logoAspectRatio`도 함께 갱신. 확대율 4.28배 → 0.44배(축소).
    브라우저에서 LG vs 한화 카드를 실제로 생성해 선명해진 것을 확인.
  - **수정 4 — 발표 문서의 사실 오류**: `docs/과제_제출_보고서.md`가 내보내기 방식을
    `pixelRatio: 1 / scale`이라 설명했으나, 실제 코드(`CanvasStage.tsx`)는 Stage를 원본 크기·배율
    1로 임시 전환한 뒤 `pixelRatio: 1`로 캡처한다(옛 방식은 부동소수점 오차 때문에 교체됨).
    채점자가 코드와 대조하면 어긋나므로 실제 구현대로 고쳤다.
  - **문서 구조 수정**: `## 5. 결정 기록` 헤딩이 두 개로 중복되고 agy 작업 로그가 §5 안에
    들어가 있어, 로그는 §4로 옮기고 헤딩을 하나로 합쳤다(§0이 경고하는 절 손상 사례).
  - **오탐 1건 — 기록해 둠**: 검토 중 "워터마크가 아예 렌더링되지 않는다"고 판단했으나, 원인은
    코드가 아니라 **테스트 환경**이었다. 이 세션의 Browser 창이 `document.visibilityState === "hidden"`
    상태라 Chrome이 이미지 디코딩을 미루고, `use-image@1.1.4`가 `img.decode()` 완료를 기다리는
    구조라 `status`가 영원히 `"loading"`에 멈춘다(직접 `img.decode()`를 호출해 3초간 settle되지
    않음을 확인). 창이 보이는 정상 사용 환경에서는 재현되지 않으므로 코드 수정은 하지 않았다 —
    **앞으로 이 프로젝트를 숨겨진 브라우저 창으로 검증할 때 이미지가 안 보이면 이 항목을 먼저 볼 것.**
  - 재검증: `npx tsc -b`·`npm run lint`(oxlint)·`npm run build` 모두 통과, 10개 구단 비율 전수 재확인.

- 2026-08-25: **Claude — 과제 제출 보고서 PPT/PDF 변환 및 증거 스크린샷 9종 확보.**
  - **증거 촬영 도구**: Browser pane은 창이 숨겨져 스크린샷이 불가능하므로(위 오탐 항목 참고)
    **Chrome DevTools MCP**로 실제 창을 띄워 1440×900에서 촬영했다. 이 환경에서는 이미지 디코딩도
    정상이라 로고·워터마크가 모두 렌더링된다.
  - `docs/evidence/` 신설, 9종 확보:
    - `card1_editor_text_style.png` — 편집기 3분할 UI + 텍스트 스타일 패널(색상·외곽선)
    - `card2_ratio_4x5.png`, `card2_ratio_9x16.png` — 비율 전환 미리보기
    - `card3_stroke_before_after.png` — `fillAfterStrokeEnabled` 하나만 바꿔 촬영한 전/후 비교
      (끄면 외곽선이 글자 속을 덮어 판독 불가, 켜면 정상 — 결함이 확실히 드러남)
    - `card4_template_crud.png` — 기본 3종 + "내가 만든 카드 A" 저장해 4개
    - `card5_json_defense.png` — 깨진 JSON 가져오기 시 에러 배너 + 템플릿 4개 그대로 보존
    - `final1_baseball.png`, `final2_meme.png`, `final3_notice.png` — 완성본 3종(각 1080×1080)
  - **내보내기 해상도 실측**: 1:1/4:5/9:16을 앱과 동일 경로로 내보내 1080×1080 / 1080×1350 /
    1080×1920이 오차 0px로 나오는 것을 확인하고 표로 슬라이드에 넣었다.
  - **보고서 사실 오류 3건 추가 정정**(제출물이라 코드와 대조):
    - "데이터 스토어(Zustand)에 원자적으로 반영" → **Zustand는 이 프로젝트에 설치조차 되어 있지
      않다**(package.json·src 전수 grep 0건, React `useState` 사용). "localStorage 저장과 React
      상태 교체를 함께 처리"로 수정.
    - "기술 스택: 순수 HTML/CSS/JS (React, Vite, Konva)" → 자기모순이고 TypeScript 프로젝트라
      "React 19 + TypeScript + Konva + Vite, 별도 상태관리 라이브러리 없음"으로 수정.
    - "자체 점검 스크린샷 20종 확보" → 실제 확보분인 "완성본 3 + 캡처 6 = 9종"으로 수정.
  - **변환**: `@marp-team/marp-cli`로 `docs/과제_제출_보고서.pptx`(17장)와 `.pdf` 생성.
    `--allow-local-files` 필요(로컬 증거 이미지 참조). 슬라이드를 PNG로도 뽑아 이미지·캡션이
    잘리지 않는지 육안 확인 후 이미지 폭을 조정(680/340/600/270px)했다.
  - 재생성 명령: `cd docs && npx @marp-team/marp-cli@latest 과제_제출_보고서.md --pptx --allow-local-files -o 과제_제출_보고서.pptx`
    (PDF는 `--pdf`로 동일)
  - **표 글자 안 보임 수정 (사용자 지적)**: 덱 frontmatter가 전역 `color: '#ffffff'`인데 Marp 기본
    테마의 표 본문(`td`)은 밝은 배경이라, 흰 글자가 흰 배경에 묻혀 표 내용이 전혀 보이지 않았다
    (헤더 `th`만 어두워서 읽혔다). `td`/`th`에 어두운 배경·밝은 글자·테두리를 명시하고 짝수 행만
    살짝 다른 톤을 주도록 style 블록을 보강했다. CARD 2 증거표·CARD 3 검사표·FINAL STATUS 표
    3곳 모두 슬라이드 PNG로 다시 뽑아 육안 확인.
  - **함께 고친 앱 결함(별개)**: 위 지적을 처음에 앱 화면 문제로 이해해 조사하다가, 실제로
    `createProject.ts`의 기본 캔버스 배경이 `#ffffff`인데 `App.tsx handleAddText()`의 기본
    글자색도 `#ffffff`라 **새 텍스트가 흰 배경에 묻히는** 같은 성격의 결함을 발견했다(검은 외곽선
    덕에 윤곽만 보이던 상태). `src/utils/textDefaults.ts`를 새로 만들어 배경의 WCAG 상대 휘도로
    밝기를 판정하고 밝은 배경 → `#111111` 글자 + 흰 외곽선, 어두운 배경 → 흰 글자 + 검은 외곽선을
    기본값으로 주도록 했다. 색을 해석할 수 없으면(그라디언트 등) 기존 흰 글자 조합을 유지한다.
    7가지 배경(흰색/3자리 hex/아이보리/검정/두산 남색/KIA 빨강/oklch)으로 검증 통과, 브라우저에서
    흰 캔버스에 텍스트 추가 시 `fill=#111111`로 나오는 것까지 확인.

- 2026-08-25: **Claude — 발표자료 전면 재작성 (17장 → 32장), 증거 15종.** 사용자가 "빠진 내용이
  많다, 지금까지 작성된 모든 문서를 확인하고 반영해 증거자료를 확보해 제작"하라고 요청.
  - **누락 진단**: `docs/00_과제_요구사항_매핑.md`를 읽어보니 기준 구조(이전 과제 25쪽 PDF)의
    필수 요소가 대거 빠져 있었다 — ① 자체 점검 **20개 항목 매핑표** ② **트러블슈팅 3건**
    ③ 검증 안내서의 **4개 필수 헤딩**(어디로/무엇을 3단계/통과 기준/안 될 때) ④ 기능 안내서
    ⑤ 야구 탭(구현했는데 발표자료에 없었음) ⑥ **배포 링크**.
  - **추가 증거 6종 촬영**(Chrome DevTools MCP, 1440×900):
    - `ev01_image_upload.png` — 1600×1200 PNG 업로드 → cover 배치 + 문구
    - `ev02_gif_rejected.png` — GIF 거부 배너 + 기존 레이어 2개 보존
    - `ev03_persist_after_reload.png` — F5 후 템플릿 4개 유지(항목 13)
    - `ev04_json_missing_fields.png` — **두 번째 방어 유형**(필수값 누락) 거부 + 4개 보존
    - `ev05_baseball_tab.png` — 야구 탭 5경기 조회 + 생성된 스코어보드 카드
    - `ev06_deployed_site.png` — **배포본에서 서버리스 API까지 정상 동작** 확인
    → 기존 9종과 합쳐 `docs/evidence/` 총 **15종**
  - **추가 검증**: 비밀값 전수 grep(`api_key|secret|password|token|Bearer|AKIA`) **매치 0건**,
    배포 URL HTTP 200 확인.
  - **덱 구성(32장)**: 타이틀(배포·소스 링크) → 개요 → 기능 안내서 → 카드 1~5(각 설명 + 증거) →
    템플릿 데이터 항목 정의 → 보너스 야구 탭 → 배포 → TECH DECISION → **트러블슈팅 3건**
    (배포 500 ESM 확장자 / ShapeLayer 미렌더링 근본수정 / 웹폰트 조용한 폴백) → AI 3줄 →
    **검증 안내서 2장(4개 헤딩)** → **20개 매핑표 2장** → FINAL STATUS.
  - 슬라이드를 PNG로 뽑아 표·이미지가 잘리지 않는지 확인 후 `--pptx`/`--pdf` 생성.

- 2026-08-25: **Codex — 프로젝트 문서 최신화.** Claude·agy의 최종 리뷰·제출 자료 제작·푸시 완료
  상태를 기준으로 `README.md`, §2 상태 스냅샷, §3 체크리스트, §6 인계를 갱신했다.
  `docs/00_과제_요구사항_매핑.md`의 20개 항목을 최종 증거와 연결하고, 누락됐던 기획·배포·검증·
  트러블슈팅·AI 3줄·포트폴리오 문서와 Codex/Antigravity 진입 파일을 최소 중복 구조로 생성했다.
  문서 상대 링크 검사와 `git diff --check`를 통과했으며 코드·PPTX·PDF·증거 이미지는 변경하지 않았다.

## 5. 결정 기록

- **수익모델은 과제 범위에서 제외, 기록만 유지 (2026-08-23)**: 사용자 요청. PRD 40장의
  10개 수익모델은 원래도 전부 `미적용`이었다. 표는 향후 검토용 기록으로 남기되 광고 스크립트·
  결제 연동·구독 관리·프리미엄 잠금 등 어떤 수익화 코드도 넣지 않는다.
- **백엔드는 야구 데이터 프록시 1개로 한정 (2026-08-23)**: 백엔드가 필요한 이유는 두 가지뿐이다.
  (1) 브라우저에서 KBO 사이트를 직접 호출하면 CORS로 차단된다. (2) 원본 응답 형식이 바뀌어도
  프론트를 고치지 않도록 서버에서 `KboGame`으로 정규화한다. 그 외에는 DB·스케줄러·계정을 두지 않고,
  이미지 편집은 전부 브라우저 안에서 끝낸다(PRD 24장 원칙 유지).
- **야구는 별도 탭이되 편집기는 공통 (2026-08-23)**: 탭은 *이미지 소스만* 전환한다.
  야구 탭의 역할은 "경기 선택 → `CanvasProject`에 배경·문구 레이어 채우기"까지고, 그 이후
  편집·미리보기·다운로드는 일반 카드와 같은 화면을 쓴다. 독립 화면으로 분리하면 템플릿·내보내기·
  히스토리 로직이 두 벌이 되어 유지보수 비용이 두 배가 된다. 기본 탭은 일반 카드.
- **KBO 데이터는 크롤링으로 확보 (2026-08-23)**: 무료 공개 API가 없고 상용은 유료다.
  사용자가 크롤링을 허용해 서버리스 프록시 방식으로 진행한다. 가져오는 값은 경기 결과(사실 정보)로
  한정하고 기사·사진 등 창작물은 가져오지 않는다. 조회는 사용자가 요청한 날짜에 한한다.
- **구단 CI 엠블럼 사용 (2026-08-23)**: 과제 제출용 **비상업적 이용** 전제로 각 구단 공식 CI
  페이지의 엠블럼을 쓴다. 원본을 변형하지 않고, 출처·취득일을 `public/logos/SOURCES.md`에 남기며,
  저작권 안내 화면에 권리 귀속 문구를 표시한다. 생성형 AI로 로고를 그리지 않는다(글자·로고가 뭉개진다).
  상업적 전환 시 구단 허락이 필요하므로, 로고 렌더링은 스코어보드 렌더러 한 곳에 모아 되돌리기 쉽게 한다.
- **스택은 PRD 22장 추천안 그대로 (2026-08-23)**: React + TypeScript + Konva.js + Vite.
  미리보기와 내보내기가 같은 Konva Stage를 쓰도록 구조를 잡는다 — PRD 42장이 지목한 최대
  기술 위험(화면과 다운로드 파일 불일치)을 나중에 맞추는 것보다 구조로 막는 편이 싸다.
- **화면 시안은 다크 워크스페이스 방향, 정지된 목업 (2026-08-23)**: 사용자 선택.
  에디터 전체가 어둡고 캔버스만 밝게 떠 있어 제작 중인 카드가 화면에서 가장 밝은 요소가 된다.
  동작하는 프로토타입 대신 정지 목업 — 진짜 동작은 Phase 3에서 React로 만들므로 시안 코드는 버려진다.
- **시안 폰트는 IBM Plex Sans KR (2026-08-23)**: 시안 캔버스는 Google Fonts만 불러올 수 있고,
  Pretendard는 거기 없다. Noto Sans KR·Inter·Roboto는 흔해서 피했다. 실제 구현 폰트는
  라이선스(OFL 등)를 확인해 다시 정한다.
- **Phase 3 CSS 변수는 시안 색 토큰을 그대로 복사 (2026-08-23)**: `src/index.css`의
  `--color-bg`, `--color-accent` 등은 `design/*.dc.html`의 인라인 oklch 값을 그대로 옮긴 것.
  시안을 나중에 고치면 이 변수도 같이 갱신해야 한다(따로 관리하는 곳 없음, 이 파일 하나가 기준).
- **이미지는 cover 방식으로 캔버스 전체를 채움 (2026-08-23)**: F-01 첫 구현에서 업로드한
  이미지를 캔버스 전체 크기에 비율 유지하며 꽉 채우는 방식(비율이 안 맞으면 넘치는 부분은 캔버스
  밖으로)을 기본값으로 정했다. 자르기(F-01-3)는 P1이라 아직 구현하지 않았고, 사용자가 나중에
  위치·크기를 직접 조절(F-01-4, F-01-5)하는 것으로 보완한다.
- **내보내기는 미리보기 Stage를 그대로 사용 (2026-08-24, 구현 방식 후속 개선)**: 별도 렌더러를
  만들지 않는 원칙은 유지한다. 최초 `pixelRatio: 1 / scale` 방식은 부동소수점 1px 오차 때문에,
  내보내기 순간 Stage를 원본 크기·배율 1로 전환하고 `pixelRatio: 1`로 캡처하는 방식으로 교체했다.
  선택 Transformer는 파일에 포함되지 않도록 숨기며 JPG는 PRD 기본값인 품질 90%로 생성한다.
- **템플릿 JSON은 버전 1 묶음으로 원자적 교체 (2026-08-24)**: 내보내기 구조는
  `{ version: 1, templates: [...] }`이며 가져오기 시 모든 템플릿과 레이어의 필수 필드를 먼저 검증한다.
  하나라도 잘못되면 저장소를 전혀 변경하지 않는다. 정상 파일은 전체 목록을 교체해 다른 브라우저에서
  내보낸 상태를 정확히 복원한다.
- **비율별 내보내기는 Stage 원본 크기 임시 전환 방식으로 오차 0% 달성 (2026-08-24)**: 1:1, 4:5, 9:16
  내보내기 시 부동소수점 스케일 오차로 1px 차이가 발생하지 않도록, `toDataURL` 실행 순간 1:1 원본
  크기로 Stage를 임시 렌더링하여 정확한 픽셀 크기(1080×1080, 1080×1350, 1080×1920)를 보장한다.
- **Phase 4(야구 탭)는 제출 마감 이후로 순연 (2026-08-24)**: 사용자가 전달한 20개 자체 점검 항목은
  카드 1~5(일반 카드뉴스)만 다루고 야구 탭(PRD 43·44장)을 포함하지 않는다. 채점에 필요 없는 범위를
  먼저 만들다가 제출 문서·모바일 정돈 같은 채점 관련 작업이 늦어지는 것을 막기 위해 순서를 미룬다.
  PRD 설계(43·44장)는 이미 완료되어 있어 착수 자체는 언제든 가능하다.
- **Phase 5(배포)는 실행을 보류, 설정 점검까지만 (2026-08-24)**: Vercel 커넥터가 이 세션에서
  인증되지 않았다. 배포는 외부 시스템에 영향을 주는 되돌리기 어려운 작업이라 사용자의 대화형 인증
  (`vercel login` 등) 없이 임의로 진행하지 않는다.
- **모바일 패널은 데스크톱 DOM을 재사용해 반응형으로 전환 (2026-08-24)**: 별도 모바일 편집기를
  복제하지 않고 900px 이하에서 기존 레이어/템플릿 패널을 오버레이로, 텍스트 스타일 패널을
  peek/expand 바텀시트로 바꾼다. 편집 상태와 핸들러가 한 벌이므로 PC·모바일 기능 불일치를 방지한다.
- **Phase 4 "제출 마감 이후로 순연" 결정을 사용자가 뒤집음 (2026-08-24)**: 위 항목은 채점 우선순위
  판단이었을 뿐 금지 규칙이 아니었다. 사용자가 "배포는 직접 하겠다, 페이즈 4로 해달라"고 명시적으로
  요청해 그 자리에서 착수했다. 앞선 결정 기록은 삭제하지 않고 그대로 두되(§0 프로토콜: 결정은
  뒤집지 않되 새 요청이 있으면 새 결정을 추가한다) 이 항목이 최신 우선순위임을 밝힌다.
- **KBO 데이터 소스는 `ws/Schedule.asmx/GetScheduleList` 실사용 웹서비스로 확정 (2026-08-24)**:
  KBO 공식 사이트가 자체 일정 화면에서 쓰는 비공개 웹서비스를 실제 요청으로 검증했다(2026년 8월
  실데이터로 종료/취소/예정 3개 상태 마크업 전부 확인). 월 단위로만 조회되어 서버가 요청 날짜가 속한
  달을 통째로 가져와 해당 날짜만 걸러낸다. 비공식 API라 KBO가 화면을 바꾸면 깨질 수 있음을
  `api/_kboClient.ts` 상단 주석에 남겼다 — PRD 44장이 요구하는 "응답 정규화" 계층이 바로 이 완충 지점.
- **구단 엠블럼은 각 구단 개별 CI 페이지 대신 KBO 공식 사이트 CDN에서 통일 수집 (2026-08-24)**:
  최초 결정 기록(43·44장 정책)은 "각 구단 공식 CI 페이지"였으나, 조사 결과 KBO 리그 공식 사이트
  (`koreabaseball.com`)가 10개 구단 엠블럼을 동일한 CDN 경로에서 통일된 포맷으로 이미 서비스하고
  있음을 확인했다. 10곳을 개별 조사하는 대신 이 단일하고 검증 가능한 공식 출처를 썼다. 최대 해상도가
  64×41px로 낮아 스코어보드에서 폭 120px 이하로만 쓴다 — 자세한 근거는 `public/logos/SOURCES.md`.
- **캔버스 렌더러의 도형(ShapeLayer) 미지원은 발견 즉시 근본 수정 (2026-08-24)**: 스코어보드 배경이
  단색으로만 보이는 결함을 야구 카드 기능이 아니라 `CanvasStage.tsx`가 애초부터 `type: "shape"`
  레이어를 그리지 않던 기존 결함으로 진단했다. 야구 기능 쪽에서 도형 대신 이미지로 우회하는 대신,
  모든 레이어가 거쳐가는 공용 렌더러에 `Rect`/`Circle` 케이스를 추가해 근본 원인을 고쳤다 — 앞으로
  어떤 기능이 도형 레이어를 써도 정상 동작한다.
- **`api/*.ts`의 상대 import는 확장자를 반드시 명시 (2026-08-24)**: Vite 로컬 dev 서버는 확장자 없는
  상대 import(`from "./_kboClient"`)를 문제없이 처리하지만, Vercel이 실제로 실행하는 컴파일된 Node
  ESM 런타임은 상대 import에 파일 확장자를 요구한다. 이 차이 때문에 로컬 검증을 통과한 코드가 배포
  후 500(`FUNCTION_INVOCATION_FAILED`)으로 죽는 사각지대가 생겼다. 앞으로 `api/` 아래에 파일을
  새로 추가하거나 상대 import를 쓸 때는 항상 `.js` 확장자를 붙인다(예: `from "./foo.js"`, 소스가
  `.ts`여도 컴파일 후 파일명과 맞춰야 한다). 배포 전 가능하면 `tsc`로 컴파일한 산출물을 직접
  `node`로 실행해 재현해보는 편이 `npm run dev`만으로는 잡히지 않는 이런 결함을 막는다.

- **구단 엠블럼 타일은 투명 배경 추출 없이 전체를 그대로 사용 (2026-08-25)**: 2026-08-24 로그가
  "투명 배경 크롭을 권장하되 다음 작업자가 실제로 비교해서 정한다"고 미뤄둔 결정. 실제로 비교한 결과
  각 타일 배경색이 이미 `teams.ts`의 구단 상징색과 거의 일치해 스코어보드 팀 배경 위에 얹어도 이중
  배경 위화감이 없었고, 타일 배경이 단색이 아니라 그라디언트·대각선 장식이라 자동 배경 제거가
  부정확할 위험이 컸다. 그래서 그리드 경계만 정확히 잘라 타일 전체(320×128px)를 그대로 쓰는 더
  단순하고 안전한 방식을 최종 채택했다.

- **로고는 흰색 배지 플레이트 위에 올려 팀 배경색과 분리 (2026-08-25)**: 2026-08-25 앞선 결정이
  "타일 배경색이 구단 상징색과 거의 일치해 이중 배경 위화감이 없다"고 판단했으나, 실제로 사용자가
  생성한 카드에서 KIA(HT)처럼 로고 타일이 그라디언트+대각선 장식인 경우 단색 배경과 뚜렷이
  어긋나 보임을 사용자 스크린샷으로 확인했다. 근본적으로 투명 배경 추출 없이는 어떤 팀 배경색과도
  100% 일치시킬 수 없으므로, 아예 로고를 흰색 둥근 배지 위에 올려 "의도된 뱃지"로 보이게 하는
  쪽으로 방향을 바꿨다. 크기도 카드 폭의 14%→28%로 키워 존재감을 줬다. 투명 배경 크롭은 여전히
  하지 않는다(그라디언트·사선 장식 때문에 자동 크로마키가 부정확할 위험은 그대로).
- **MVP 선수 사진 기능 — 조사·구현까지 마쳤으나 사용자가 최종 포기 (2026-08-25)**: 여러 차례
  확인을 거쳐 진행 경로가 좁혀졌다.
  1. KBO 자체 "키플레이어" 데이터를 발견 — `POST ws/Schedule.asmx/GetKeyPlayerHitter`/
     `GetKeyPlayerPitcher`(groupSc=GAME_WPA_RT)가 경기별로 팀 구분 없이 WPA(승리기여도) 랭킹과
     선수 사진(`person/kbo/{year}/{playerId}.png`)을 준다. KBO에 "MVP"라는 공식 명칭은 없어
     "이 경기 최고 기여 선수"로 표현하기로 했다. 이 사진은 91×91~94×118px로 작다.
  2. 사용자가 화질 문제로 "구단 홈페이지 사진을 쓰자"(예: tigers.co.kr)고 제안 — 실제로 KIA
     공식 사이트에 440×364px 투명 배경 고화질 선수 사진이 있음을 확인했다.
  3. 그러나 실제 서버 사이드에서 확보를 시도하니 막혔다: (a) `tigers.co.kr/players/*` 페이지는
     클라이언트 렌더링(SPA)이라 `fetch()`로 받은 원본 HTML이 빈 셸(2.8KB, 선수 정보 0건)이었고,
     (b) 실제 데이터를 주는 내부 API `POST tigers.co.kr/v1/game/playerlist`는 curl로 직접
     호출하면 매번 타임아웃 — 브라우저가 아닌 요청을 WAF가 막는 것으로 판단. Vercel 서버리스
     환경도 마찬가지로 막힐 가능성이 높아 채택하지 않았다.
  4. 사용자가 "해당 경기의 실제 보도사진(뉴스 사진)"도 제안했으나, tigers.co.kr 뉴스 목록은
     gameId와 연결된 구조화 데이터가 아니라 어떤 기사가 어느 경기 것인지 자동으로 정확히 판별할
     방법이 없고, 언론사 저작권 문제도 구단 CI보다 명백히 크다고 판단해 채택하지 않았다.
  5. 결국 KBO 자체 91×91px 사진으로 팀별 원형 배지(작은 크기, 이름 캡션 포함)를 실제로 구현하고
     `npx tsc -b`·`npm run lint` 통과까지 확인했으나, **사용자가 "중지하고 사진 사용하는 건
     포기할게"라고 최종 결정**해 관련 코드를 전부 되돌렸다(`api/_kboKeyPlayerClient.ts`,
     `api/kbo-keyplayer.ts`, `src/features/baseball/fetchKeyPlayers.ts` 삭제,
     `buildScoreboardLayers.ts`·`BaseballTab.tsx`·`vite.config.ts`·`src/types/baseball.ts`
     원상복구). 로고 배지 수정(§5 위 항목)은 이 되돌리기와 무관하게 그대로 유지된다.
  6. 부산물로 남긴 것: `ImageLayerNode.tsx`의 cover-fit crop(2026-08-23 결정 "이미지는 cover
     방식으로 채움"이 실은 미구현이었던 부분을 이번에 실제로 구현, 일반 이미지 업로드에도 적용됨),
     `ShapeLayer.cornerRadius`, `BaseLayer.opacity` 필드는 계속 유용해 되돌리지 않았다.
- **로고 워터마크 추가 — `BaseLayer.opacity` 신설 (2026-08-25)**: 사용자가 "로고를 워터마크
  형식으로 좀 크게 적용하고 싶어"라고 요청. 모든 레이어 타입에 선택적 `opacity?: number`(기본 1)를
  추가하고 `CanvasStage.tsx`/`ImageLayerNode.tsx`의 각 Konva 노드에 그대로 연결했다(하위 호환,
  기존 템플릿 JSON 영향 없음). 스코어보드에는 각 팀 배경 폭의 85%짜리 큰 로고를 배경(zIndex 0/1)
  바로 위, 다른 모든 요소 아래에 깔았다. 상단의 작은 흰 배지 로고(§5 위 항목)와는 별개로 공존한다.
  불투명도는 처음 0.16으로 시작했으나 실제로 캡처해 보니 KIA(HT) 쪽에서 로고 타일의 어두운
  배경이 사각형 경계로 비쳐 보여(§4 로그 참고) 0.08로 낮췄다.

- **구단 엠블럼 원본 종횡비 보존 contain 레이아웃 (2026-08-25, Codex/agy)**: 구단별로 가로/세로 비율이 서로 다른 엠블럼(1.00 ~ 1.70)을 강제로 동일 비율로 늘리지 않고, `KBO_TEAMS`에 `logoAspectRatio`를 명시하고 `buildScoreboardLayers.ts`의 `fitAspectRatio`를 통해 배지 플레이트 및 워터마크 영역 내에 contain 방식으로 비례 배치하여 로고의 찌그러짐을 방지한다.
- **구단 엠블럼은 워터마크 렌더 크기(513px) 이상 해상도로 확보 (2026-08-25, Claude)**: 워터마크는
  1080px 캔버스에서 최대 513px 폭으로 그려지므로, 그보다 훨씬 작은 원본을 쓰면 그 팀만 뭉개져
  보인다. LG가 120×120으로 약 4.3배 확대되던 문제를 위키백과의 LG 트윈스 로고 SVG를 1280px로
  렌더해 교체(1174×920)하여 해소했다. 앞으로 엠블럼을 교체할 때는 513px 이상(최소 2배 확대 이내)을
  기준으로 삼는다 — 현재 최소는 OB 244px(약 2.1배).
- **Konva 블렌드 모드(`globalCompositeOperation`)는 노출하지 않는다 (2026-08-25, Claude)**:
  값을 설정하는 코드가 하나도 없는데 타입만 자유 문자열로 열려 있었고, `ImageLayerNode`가 `as any`로
  캐스팅해 넘기고 있었으며, `templateStore`의 가져오기 검증도 이 필드를 보지 않았다. 가져온 템플릿
  JSON이 `"destination-out"`을 넣으면 아래 레이어를 지워버릴 수 있어(과제 카드 5가 방어하기로 한
  경로) 필드 자체를 제거했다. 실제로 블렌드 모드가 필요해지면 안전한 값만 허용하는 유니온 타입과
  `templateStore` 검증을 함께 추가한다.

## 6. 다음 작업자에게

**다음 작업자:** 사용자 — 최종 제출 또는 선택적 개선 판단

### 현재 인계 상태 (2026-08-25)

- 개발·브라우저 검증·배포·코드리뷰·제출용 PPTX/PDF 제작·Git 푸시까지 완료했다.
- 기능·제출 산출물 기준 커밋은 `c75185d`이며 문서 정리 시작 전 `main == origin/main`, 작업 트리
  clean을 확인했다. 이후 문서 최신화 커밋은 `git log -1`로 확인한다.
- 제출 파일은 `docs/과제_제출_보고서.pptx`와 `docs/과제_제출_보고서.pdf`이다.
- 기능상 필수 후속 작업은 없다. §7의 시안 캔버스 저장은 구현·제출과 분리된 사용자 선택 절차다.
- 추가 변경을 시작할 때는 새 요청을 작업 단위로 정의하고 기존 제출 산출물을 보존한다.

## 7. 미해결 질문

- ~~실제 구현에 쓸 한글 폰트 미확정~~ **해결 (2026-08-25, Claude)**: IBM Plex Sans KR은 IBM이
  SIL Open Font License 1.1로 배포하는 오픈소스 폰트라 라이선스 문제 없음. 단, 코드 전수 검색 결과
  `index.html`에 Google Fonts 링크도, npm 패키지도, 로컬 폰트 파일도 없어 **실제로는 한 번도
  로드된 적이 없었고** 브라우저가 조용히 `Malgun Gothic`으로 폴백하고 있던 결함을 발견해 수정함.
  상세는 §4·§5 참고.
- 시안 캔버스 도구에 4장을 저장하고 사용자가 최종 확인하는 절차가 남아있다 — 지금은 시안 HTML
  파일만 전달된 상태. **AI가 대신 완료할 수 없음** — 실제 시안 캔버스 도구 조작과 사용자 본인의
  최종 확인이 필요하다.
- ~~이미지 업로드 용량 상한을 15MB로 임시 설정함~~ **검증 완료 (2026-08-25, Claude)**: 브라우저에서
  4500×4500 노이즈 PNG(77.55MB, 실제 사진보다 훨씬 나쁜 최악의 경우)를 생성해 `Image()` 디코드
  시간을 측정한 결과 약 90ms로 사실상 즉시 처리됨. 15MB는 디코드 성능상 전혀 문제되지 않는 여유
  있는 상한이므로 그대로 유지하기로 함(코드 변경 없음).
- ~~Vercel 커넥터가 인증되지 않은 상태다~~ **이미 해소됨 (확인만, 2026-08-25)**: Phase 5(배포)가
  이미 완료되어 https://card-news-maker-beige.vercel.app/ 가 운영 중이고 `/api/kbo-schedule`도
  정상 응답 중이다(§2 참고). 이 질문은 Phase 5 착수 이전 시점의 낡은 항목이었다.

## 8. 하지 말 것

- 수익화 기능(광고·결제·구독·프리미엄 잠금)을 만들지 않는다.
- 사용자 이미지를 서버로 보내지 않는다.
- 회원가입·로그인·클라우드 저장·협업 편집을 먼저 만들지 않는다 (PRD 42장).
- 야구 탭을 독립 화면으로 분리하지 않는다 (결정 기록 참고).
- `PRD.md`를 통째로 읽지 않는다.
