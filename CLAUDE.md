# card-news-maker 작업 규칙 (Claude Code 세션 시작 시 자동 로드)

> 이 폴더에서 작업할 때 **폴더 전체를 read하지 말 것.** 아래 규칙을 따를 것.

## 0. 새 프로젝트 시작 시 만들 문서 세트

프로젝트를 새로 시작하면, 코드보다 먼저 아래 표준 문서 세트를 만든다:

| 파일명 | 용도 |
|---|---|
| `README.md` | 프로젝트 개요·규칙·폴더 구조·문서 색인 |
| `작업내역_체크리스트.md` | **모든 AI 모델의 공용 인수인계 파일.** 진행 상황·결정 기록·작업 로그를 단일 파일에 누적 기록 |
| `docs/01_기획.md` | 기술 스택·시스템 구조·알고리즘 등 설계 내용 |
| `docs/05_배포.md` | 배포처 비교·선택 근거·배포 절차 |
| `docs/00_과제_요구사항_매핑.md` | 과제가 내준 항목 전부 — 필수 항목과 학생 점검 항목을 구분하여 1:1 매핑 |
| `docs/검증안내서.md` | 어디로 가서 무엇을 하면 무엇이 보여야 통과인지 |
| `docs/트러블슈팅.md` | 실제로 겪은 문제·시도·비교·해결 및 배운 점 |
| `docs/AI_3줄.md` | AI에게 맡긴 일 / 내가 판단한 일 / AI 말을 안 들은 일 |
| `docs/포트폴리오_추가용_소개글.md` | 개인 포트폴리오에 프로젝트 추가 시 즉시 활용할 소개글 |
| `CLAUDE.md` (본 문서) | AI가 폴더를 통째로 읽지 않도록 하는 토큰 절약 규칙 및 프로젝트 실행 지침 |

## 0-1. 각 AI 툴에 규칙을 자동으로 읽히기

| 툴 | 자동으로 읽는 파일 |
|---|---|
| Claude Code | `CLAUDE.md` |
| Cursor | `.cursorrules` 또는 `.cursor/rules/` |
| Gemini CLI / agy | `GEMINI.md` |
| OpenAI Codex | `AGENTS.md` |

폴더 전체를 훑어보지 말고, **`작업내역_체크리스트.md`** 한 파일만 먼저 읽는다.

## 1. 세션 시작 시 가장 먼저 할 일

폴더 목록을 훑어보는 대신, **`작업내역_체크리스트.md` 한 파일만** 먼저 읽는다.
그 파일 안의 `0. AI 필독 — 작업 프로토콜` 섹션이 이후 작업 순서(시작 전/중/종료 시)를 전부 정의한다.

## 2. 절대 통째로 읽지 말 것 (토큰 낭비)

| 경로 | 이유 |
|---|---|
| `PRD.md` | **95KB·44장짜리 개발요청서.** 절대 전체를 읽지 않는다. `grep "^## "`로 목차를 본 뒤 필요한 장만 잘라 읽는다 |
| `design/*.dc.html` | 화면 시안 HTML. 한 장이 수백 줄이고 인라인 스타일 덩어리다. 고칠 아트보드 1개만 연다 |
| `design/*.html` (시안 캔버스 산출물) | 조립된 결과물. **읽지 않는다** — 편집은 항상 `design/*.dc.html` 원본에서 한다 |
| `public/logos/` | 구단 엠블럼 바이너리. 내용 확인은 필요할 때만. 출처는 `public/logos/SOURCES.md`에 정리돼 있다 |
| `src/` 전체 | 작업과 관련된 파일 1~2개만 골라 읽는다. 디렉토리 전체를 한 번에 읽지 않는다 |
| `node_modules/`, `dist/`, `.git/` | 의존성·빌드 산출물. 원칙적으로 읽지 않음 |

## 3. 작업 유형별로 이 문서 하나만 읽기

> **💡 과제 체크리스트 수신 원칙**: 새로운 과제 시작 시 사용자가 프롬프트로 제공한 과제 체크리스트를 바탕으로 `docs/00_과제_요구사항_매핑.md`를 작성하고 이를 기준으로 구현 및 검증을 진행합니다.

| 하려는 작업 | 읽을 문서 |
|---|---|
| 과제 채점 기준·체크리스트 확인 | `docs/00_과제_요구사항_매핑.md` |
| 진행 상황·다음 할 일 | `작업내역_체크리스트.md` |
| 기능 요구사항·화면 설계 확인 | `PRD.md`의 해당 장만 (13·19장) |
| 야구 탭 사양 | `PRD.md` 43장 |
| 백엔드 API 사양 | `PRD.md` 44장 |
| 폴더 구조·문서 색인 | `README.md` |
| 과거 문제 해결 사례 | `트러블슈팅.md` |
| 배포 방법·절차 | `docs/배포.md` |

## 4. 코드 수정 시

- 어떤 파일을 고쳐야 하는지 알고 있다면 그 파일만 연다 (예: `src/features/image/upload.ts`).
- 모를 때만 `src/` 하위 디렉토리 목록을 보되, 목록만 보고 내용을 전부 읽지는 않는다.
- 기존 파일을 통째로 다시 쓰지 않는다 — 필요한 부분만 수정한다.

- **코드를 고쳤으면 그 작업은 아직 끝난 게 아니다.** 관련 문서를 갱신해야 작업이 종료된 것으로 친다.
  - `작업내역_체크리스트.md`의 작업 로그·진행사항 체크리스트에 반영
  - 동작이나 구조가 바뀌었으면 `README.md`, `PRD.md`, `docs/배포.md` 등도 같이 수정
  - "나중에 한꺼번에 정리" 하지 않는다 — 코드 변경과 문서 갱신을 같은 작업 단위로 묶는다

### 4-1. 커밋 전 문서 갱신 체크리스트

- [ ] 새 기능/동작을 추가했다 → README·PRD 갱신했는가
- [ ] 구조·파일 위치가 바뀌었다 → README의 폴더 구조 갱신했는가
- [ ] 배포 방식·설정이 바뀌었다 → `docs/배포.md` 갱신했는가
- [ ] 문제를 발견하고 해결했다 → `트러블슈팅.md`에 기록했는가
- [ ] 새로운 결정을 내렸다 → `작업내역_체크리스트.md`의 결정 기록에 추가했는가
- [ ] 어쨌든 뭔가 했다 → `작업내역_체크리스트.md`의 작업 로그에 남겼는가

### 4-2. 작업 완료 및 인계 시 필수 규칙 (Next Agent & Prompt)

작업을 완료하고 결과를 보고할 때는 다음 사항을 반드시 지킨다:
1. **다음 작업자(Next Agent)와 수행할 구체적 작업 명시**: 다음 작업을 진행할 AI 모델(Codex/agy/Claude 등) 또는 사용자를 지정하고 진행할 작업을 명확히 정의한다.
2. **다음 작업자용 실행 지시 프롬프트 제공**: 다음 AI가 세션을 열었을 때 복사해서 즉시 실행할 수 있도록 역할, 대상 파일, 작업 절차, 제약사항 및 검증 기준이 포함된 프롬프트를 함께 작성하여 제공한다.
3. `작업내역_체크리스트.md`의 `6. 다음 작업자에게` 절에도 이 내용을 동일하게 갱신한다.

## 5. 이 프로젝트에서 하지 말 것

- **수익화 기능을 만들지 않는다.** 광고·결제·구독·프리미엄 잠금 전부 과제 범위 밖이다 (`PRD.md` 40장 말미 참고).
- **이미지를 서버로 보내지 않는다.** 백엔드는 야구 데이터 조회 프록시 1개뿐이다 (`PRD.md` 24장 7번, 44장).
- **회원가입·로그인·클라우드 저장을 먼저 만들지 않는다** (`PRD.md` 42장).

## 6. 이 파일 자체 수정 규칙

새로 생긴 대형 폴더/파일이 있으면 2번 표에 한 줄 추가한다.
